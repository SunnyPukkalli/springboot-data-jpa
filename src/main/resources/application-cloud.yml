spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://testing.ckpmkzadtbde.ap-south-1.rds.amazonaws.com/testing
    username: testing
    password: testing
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL94Dialect