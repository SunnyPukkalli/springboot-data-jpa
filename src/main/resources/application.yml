spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://XXXXXXXX/XX
    username: XXXXXXX
    password: XXXXXXX
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL94Dialect
